<section class="container section">
<%= simple_form_for(@about, url: abouts_path, html: { class: 'form-horizontal' }) do |f| %>
<div class="row ws-m">
  <div class="col-md-offset-2 col-md-8">

  <% if @about.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@about.errors.count, "error") %> prohibited this blog from being saved:</h2>
      <ul>
      <% @about.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group" Portfoli > </div>
  <div class="form-group">
    <%= f.input :website, placeholder: "https//:" %>
  </div>
  <div class="form-group">
    <%= f.input :email, placeholder: "Type Your official email" %>
  </div>
  <div class="form-group">
    <%= f.input :signature, placeholder: "Whats Your Role & Postion" %>
  </div>
  <div class="form-group">
    <%= f.input :contact, placeholder: "Other ways of contacting You" %>
  </div>
  <div class="form-group">
    <%=  f.trix_editor :hobby, placeholder: "Whats Your Bio"  %>
  </div>
  <div class="form-group">
    <%= f.input :thumbnail, as: :file, paceholder: "Add display picture" %>
  </div>

  <div class="form-group">
    <%= f.submit :submit, class: "btn btn-ghost-light" %>
  </div>
<% end %>
</div>
</div>
</section>
